var app = angular.module('app', ['app.auth']);

app.controller('LoginController',function( $scope, $http, authService) {
    $scope.login = {};

    $scope.acessoLogin = function() {
        alert("login");
    };
});
